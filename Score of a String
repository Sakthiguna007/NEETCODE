class Solution {
    public int scoreOfString(String s) {
        int sco =0;
        for (int i=0;i<s.length()-1;i++){
          sco+=Math.abs(s.charAt(i+1)-s.charAt(i));
        }
        return sco;
        
        }
    }
